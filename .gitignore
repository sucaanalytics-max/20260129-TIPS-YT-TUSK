.vercel
.env*.local
.worktrees
node_modules/
.playwright-mcp/
.DS_Store
**/.DS_Store

# Seeded design-canvas output (~2.6MB vendored editor payload each).
# Regenerate from the .dc.html sources + canvas.json with the design skill helper.
design/*.html
!design/*.dc.html
